#include <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Description: Prints a specific text with proper grammar.
 *     * The outcome is a piece of art.
 *      *
 *       * Return: Always 0 (Success)
 *        */
int main(void)
{
		/* Using printf to display the desired text */
		printf("with proper grammar, but the outcome is a piece of art,\n");

			return (0);
}

